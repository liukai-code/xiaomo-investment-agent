# 项目结构改进路线图

> 基于 2026-06-28 项目结构审查，P0 和 P1 已完成，以下为后续改进计划。

---

## ~~P0 — 立即修正（影响代码规范）~~ ✅ 已完成

- ~~重命名 `agentLoopController.java` → `AgentLoopController.java`~~
- ~~重命名 `Entity/` 包 → `entity/`~~
- ~~重命名 `Impl/` 包 → `impl/`~~

---

## ~~P1 — 短期改进（影响可维护性）~~ ✅ 已完成

### ~~1. 重构为按功能模块分包~~ ✅

已重构为按功能模块分包，当前结构：

```
com.xiaomo.agent/
├── XiaomoApplication.java
├── common/                        # 通用基础设施
│   ├── entity/Result.java
│   ├── exception/GlobalExceptionHandler.java
│   └── config/                    # RedisConfig, WebMvcConfig, McpKeepAliveService
├── auth/                          # 认证模块
│   ├── controller/AuthController.java
│   ├── service/TokenManager.java
│   ├── service/impl/TokenManagerImpl.java
│   └── interceptor/AuthInterceptor.java
├── user/                          # 用户模块
│   ├── entity/User.java
│   └── repository/UserRepository.java
├── conversation/                  # 对话模块
│   ├── entity/                    # Conversation, ChatMessage, MessageRole
│   ├── controller/AgentLoopController.java
│   ├── repository/                # ConversationRepository, ChatMessageRepository
│   └── service/                   # ConversationService, ChatMessageService, ChatHistoryCacheService + impl
├── agent/                         # Agent 核心
│   ├── service/AgentLoop.java
│   └── service/impl/AgentLoopImpl.java
└── tool/                          # Agent 工具
    ├── config/ToolConfig.java
    ├── FileListTool.java, FileReadTool.java, FileWriteTool.java
    ├── FinancialDataTool.java
    └── SqlTool.java
```

---

### ~~2. 补充 Dockerfile + docker-compose.yml~~ ✅

已创建以下文件（位于项目根目录）：
- `Dockerfile` — 多阶段构建（node→maven→jre）
- `docker-compose.yml` — 编排 PostgreSQL 16 + Redis 7 + App
- `.dockerignore` — 排除 target/、node_modules/、.git/ 等

使用方式：
```bash
# 创建 .env 文件填入 ANTHROPIC_API_KEY 等环境变量后
docker compose up --build       # 一键启动
docker compose up --build -d    # 后台运行
docker compose down             # 停止
```

---

### ~~3. 添加环境变量模板文件~~ ✅

已创建 `application.yml.example`（位于项目根目录），包含所有环境变量占位说明。

---

## P2 — 中期改进（影响质量保障）

### 4. 补充后端单元测试

**现状：** 仅 1 个 Spring Boot context load test，核心业务逻辑无测试覆盖。

**优先测试目标：**

| 优先级 | 类 | 测试重点 |
|--------|-----|---------|
| 高 | `AgentLoopImpl` | chat()、chatStream()、generateTitle() 的核心逻辑 |
| 高 | `ChatMessageServiceImpl` | saveMessage()、getHistory()、saveAssistantMessage() |
| 高 | `ConversationServiceImpl` | createConversation()、listConversations()、checkOwnership() |
| 中 | `TokenManagerImpl` | JWT 生成/验证、BCrypt 加密 |
| 中 | `ChatHistoryCacheServiceImpl` | Redis 缓存读写、过期、失效 |
| 低 | `SqlTool` | SQL 注入防护、只读校验 |
| 低 | `FinancialDataTool` | 外部 API 调用 Mock |

**测试框架：** 已有 `spring-boot-starter-test`，可直接使用 JUnit 5 + Mockito + `@SpringBootTest`。

**建议：** 为 Service 层编写单元测试（Mock Repository），为 Controller 层编写 `@WebMvcTest` 切片测试。

---

### 5. 添加 CI Pipeline

**目标：** 每次 push/PR 自动执行构建和测试，防止回归。

**推荐方案：** GitHub Actions（项目已在 Git 管理下）。

```yaml
# .github/workflows/ci.yml
name: CI
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: cd frontend && npm ci && npm run build
      - run: mvn clean verify -B
```

---

### ~~6. 前端组件分类整理~~ ✅

已完成。通用组件移入 `components/common/`，同步更新 import 路径：

```
frontend/src/components/
├── common/                  # 通用组件
│   ├── GalaxyBackground.vue
│   └── StreamingCursor.vue
└── blocks/                  # Markdown 渲染块
    └── ...
```

---

## P3 — 长期优化

### 7. 引入前端测试框架

**推荐：** Vitest（与 Vite 原生集成，零配置）。

**优先测试目标：**
- `stores/`：auth.ts、chat.ts 的状态管理逻辑
- `utils/markdown.ts`：Markdown 解析函数
- `composables/useMarkdownBlocks.ts`：块解析逻辑

```bash
npm install -D vitest @vue/test-utils jsdom
```

---

### ~~8. 整理 docs 目录~~ ✅

已完成。文档按类型归类：

```
docs/
├── APIDocs/                 # API 接口文档
├── DevelopmentProcess/      # 开发过程设计文档
├── Planning/                # 规划方案
│   ├── 金融Agent-Tool扩充方案.md
│   ├── 金融投资导学Agent开发计划书.md
│   └── 项目结构改进路线图.md
└── Guides/                  # 使用/升级指南
    └── 金融Agent高级特性升级指南.md
```

---

### 9. 统一构建/启动脚本

**目标：** 一条命令完成常见操作，降低记忆成本。

**方案：** 在项目根目录添加 `Makefile`：

```makefile
.PHONY: dev build test clean

dev:
	cd frontend && npm run dev &
	mvn spring-boot:run

build:
	cd frontend && npm run build
	mvn clean package -DskipTests

test:
	cd frontend && npx vitest run
	mvn test

clean:
	mvn clean
	rm -rf frontend/node_modules frontend/dist target/
```

---

## 改进优先级总览

| 阶段 | 编号 | 改进项 | 工作量 | 影响 | 状态 |
|------|------|--------|--------|------|------|
| P0 | — | 命名规范修正（Entity/entity、Impl/impl、类名大驼峰） | 小 | 代码规范 | ✅ 已完成 |
| P1 | 1 | 按功能模块分包 | 中 | 可维护性 | ✅ 已完成 |
| P1 | 2 | Dockerfile + docker-compose | 小 | 部署效率 | ✅ 已完成 |
| P1 | 3 | 环境变量模板文件 | 小 | 新人上手 | ✅ 已完成 |
| P2 | 4 | 后端单元测试 | 大 | 质量保障 | 待执行 |
| P2 | 5 | CI Pipeline | 小 | 质量保障 | 待执行 |
| P2 | 6 | 前端组件分类 | 小 | 可维护性 | ✅ 已完成 |
| P3 | 7 | 前端测试框架 | 中 | 质量保障 | 待执行 |
| P3 | 8 | 整理 docs 目录 | 小 | 文档规范 | ✅ 已完成 |
| P3 | 9 | 统一构建脚本 | 小 | 开发效率 | 待执行 |
