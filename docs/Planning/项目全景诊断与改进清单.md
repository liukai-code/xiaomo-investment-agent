# 项目全景诊断与改进清单

> 基于 2026-07-16 全面代码审查，覆盖后端 137 个 Java 源文件（~15,453 行）、前端 70+ 个 TS/Vue 文件（~11,552 行）、27 个测试文件、30+ 篇文档。

---

## 一、项目现状概览

| 维度 | 状态 | 评价 |
|------|------|------|
| 核心功能 | AI Agent 对话 + 44 个金融数据端点 + 深度分析工作流 | 功能丰富 |
| 代码质量 | 包结构清晰，命名规范，无 FIXME/HACK | 良好 |
| 测试覆盖 | 工具层测试完善，核心业务层几乎空白 | 严重不足 |
| CI/CD | 完全缺失 | 需要补建 |
| 安全性 | SQL 注入防护完善，认证层有改进空间 | 中等 |
| 文档 | 30+ 篇设计/规划文档 | 完善 |
| 前端规范 | 无 ESLint/Prettier，无测试框架 | 需要补齐 |

---

## 二、功能缺口

### 2.1 对话体验增强

| 缺失功能 | 说明 | 优先级 |
|----------|------|--------|
| 消息搜索 | 对话历史只能线性浏览，缺乏关键词搜索 | 中 |
| 会话管理增强 | 缺少重命名、置顶、归档、删除确认 | 中 |
| 多模态输入 | 不支持图片上传（如 K 线截图让 AI 分析） | 低 |
| 消息操作 | 缺少单条消息的复制、重新生成（regenerate）、编辑重发 | 中 |

### 2.2 数据可视化

- A 股工具返回的 K 线、资金流、龙虎榜等数据全是纯文本/表格，前端无图表渲染
- `useFinanceChartCanvas` composable 已存在但未充分利用
- 建议接入 ECharts 或 Lightweight Charts，实现 K 线图、资金流向图等可视化

### 2.3 mootdx 通达信协议（3 个 TODO）

位置：`AStockQuoteRouterTool.java:34-36`

```
- mootdxKline: TODO 占位（mootdx TCP 协议未实现，请用 baiduKline 替代）
- mootdxQuotes: TODO 占位（请用 tencentQuote 替代）
- mootdxTransaction: TODO 占位（逐笔成交暂未实现）
```

通达信是 A 股最实时的数据源。建议决策：要么实现，要么清理掉这些占位方法。

---

## 三、工程质量问题

### 3.1 高优先级

#### P0-1: CI/CD 完全缺失

没有任何自动化构建/测试/部署流程。无 GitHub Actions、GitLab CI、Jenkinsfile 等。

**建议方案：**

```yaml
# .github/workflows/ci.yml
name: CI
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: cd frontend && npm ci && npm run build
      - run: mvn clean verify -B
```

#### P0-2: 核心模块零测试

CLAUDE.md 明确要求"每次修改或新增后端接口必须编写真实测试用例"，但以下核心模块没有任何测试：

| 模块 | 文件 | 行数 | 风险 |
|------|------|------|------|
| AgentLoopImpl | `agent/service/impl/AgentLoopImpl.java` | 785 | 对话核心循环，意图分类、工具过滤、标的锁定全部未验证 |
| WorkflowEngine | `workflow/engine/WorkflowEngine.java` | — | 多智能体工作流引擎，5 层 Pipeline 逻辑复杂 |
| DeepAnalysisWorkflow | `workflow/DeepAnalysisWorkflow.java` | — | 深度分析主流程 |
| AuthController | `auth/controller/AuthController.java` | — | 注册/登录/登出/token 校验 |
| ConversationService | `conversation/service/` | — | 会话管理和消息 CRUD |
| ChatHistoryCacheService | `conversation/service/` | — | Redis 缓存逻辑 |
| StockResolver / StockCodeExtractor | `tool/` | — | 标的解析工具类 |
| ToolConfigService | `tool/config/` | — | 工具开关机制 |
| MaxToolCallManager | `tool/guard/` | — | 工具调用守卫 |

#### P0-3: 前端零测试

- 没有 Vitest/Jest 配置，没有一个测试文件
- SSE 流式解析、Markdown 渲染、Pinia Store 等核心逻辑未验证
- 建议至少覆盖：`useMarkdownBlocks.ts`、`chat.ts`（SSE 解析）、`stores/auth.ts`

#### P0-4: 数据库迁移不完整

- 仅 2 个迁移文件：`V2__create_user_config_table.sql`、`V3__add_channel_support_to_user_config.sql`
- 缺少 V1（初始表结构迁移），依赖 JPA `ddl-auto: update` 自动建表
- 生产环境风险：`ddl-auto: update` 不会删除列/表，可能导致 schema 不一致

**建议：** 从当前数据库导出 schema，生成 `V1__init_schema.sql`，然后关闭 `ddl-auto: update`

---

### 3.2 中优先级

#### P1-1: AgentLoopImpl 代码重复

`chat()` 和 `chatStream()` 两个方法中，工具上下文构建（toolCtx）和工具过滤逻辑存在约 100 行重复代码。

**建议：** 提取 `buildToolContext()` 和 `filterTools()` 公共方法。

#### P1-2: 安全加固

| 问题 | 位置 | 建议 |
|------|------|------|
| admin 默认密码 `admin123` | `application.yml` | 生产环境必须通过环境变量覆盖，启动时校验非默认值 |
| 密码逻辑写在 Controller 层 | `AuthController` | 移至 AuthService |
| `@RequestBody Map<String, String>` 缺校验 | `AuthController` | 改为 DTO + `@Valid` |

#### P1-3: 缺少生产配置

没有独立的 `application-prod.yml`，生产/开发共用同一配置文件。

**建议：**
- 创建 `application-prod.yml`，覆盖数据库、Redis、日志级别等配置
- 关闭 `ddl-auto: update`，改用 Flyway 管理
- 关闭 H2 console、SQL 日志等调试功能

#### P1-4: 前端代码规范

缺少 ESLint/Prettier 配置，无法保证代码风格一致性。

**建议：**
```bash
cd frontend
npm install -D eslint @eslint/js eslint-plugin-vue prettier eslint-config-prettier
```

#### P1-5: Spring AI 版本风险

当前使用 `1.1.0-M3`（里程碑版本），非正式 GA 版本，API 可能变动。

**建议：** 关注 Spring AI GA 版本发布，评估升级成本。

---

### 3.3 低优先级

| 问题 | 说明 |
|------|------|
| docker-compose 缺少 Redis 健康检查 | PostgreSQL 有 `pg_isready`，Redis 没有 |
| app.log 存在于仓库根目录 | `.gitignore` 已排除但文件仍在 |
| test_temp_files 目录 | 测试产生的遗留物，确认后清理 |
| Makefile 缺失 | 无统一构建/启动脚本，新人上手成本高 |

---

## 四、优化建议

### 4.1 性能优化

| 方向 | 说明 |
|------|------|
| Redis 缓存监控 | 当前有缓存但无可观测性，建议加 metrics 统计命中率 |
| A 股工具并行化 | 深度分析 Layer1 的 3 个分析师串行调用数据接口，可并行请求不同数据源 |
| SSE 心跳机制 | 长连接缺少心跳，浏览器标签页切换后可能断连 |

### 4.2 架构优化

| 方向 | 说明 |
|------|------|
| AgentLoopImpl 拆分 | 785 行上帝类，建议拆为 ToolExecutor、ContextBuilder、StreamHandler 等 |
| 工具注册机制 | 当前通过 `@Component` + 手动注册，可考虑注解扫描自动发现 |
| Workflow 节点解耦 | 各节点（AnalystNode、DebateNode 等）可抽象为统一接口，支持配置化编排 |

---

## 五、优先行动清单

按投入产出比排序，建议按此顺序执行：

### 第一批：质量基线（1-2 天）

- [ ] 添加 GitHub Actions CI（编译 + 测试 + Docker 构建）
- [ ] 补 `V1__init_schema.sql`，关闭 `ddl-auto: update`
- [ ] 前端加 Vitest 配置 + `useMarkdownBlocks` 基础测试

### 第二批：核心测试（3-5 天）

- [ ] AuthController 测试（注册/登录/登出/token 校验）
- [ ] ConversationService 测试（CRUD + 权限校验）
- [ ] AgentLoopImpl 核心逻辑测试（意图分类、工具过滤）

### 第三批：安全与规范（1-2 天）

- [ ] 创建 `application-prod.yml`
- [ ] AuthController 密码逻辑移至 Service 层
- [ ] 参数校验 DTO 化
- [ ] 前端加 ESLint + Prettier

### 第四批：功能增强（按需）

- [ ] 消息搜索功能
- [ ] 数据可视化（K 线图等）
- [ ] mootdx 协议实现或清理
- [ ] 会话管理增强

---

## 六、测试覆盖现状

### 已有测试（27 个文件）

| 类别 | 测试文件 | 覆盖情况 |
|------|----------|----------|
| 金融计算 | `FinancialCalcToolTest`（813 行） | 全覆盖，含边界/异常 |
| A 股工具 | 8 个 Router Tool 测试 + Utils + RateLimiter | 良好 |
| SQL 工具 | `SqlToolTest` | 安全验证完善 |
| 文件工具 | Read/Write/List 三个测试 | 基本覆盖 |
| 深度分析 | Controller + Service 测试 | 有覆盖 |
| 意图分类 | `RuleBasedIntentClassifierTest` | 有覆盖 |
| 通知系统 | Controller + Service 测试 | 有覆盖 |
| 用户配置 | `UserConfigServiceTest` | 有覆盖 |
| 用量记录 | `UsageRecordServiceTest` | 有覆盖 |
| 应用启动 | `MyClaudeCodeApplicationTests` | context load |

### 缺失测试

见 [P0-2: 核心模块零测试](#p0-2-核心模块零测试)。

---

*本文档将随项目迭代持续更新。*
